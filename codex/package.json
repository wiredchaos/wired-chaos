{
  "name": "@wiredchaos/codex",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "bootstrap": "pnpm install && pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev:web": "pnpm -C packages/codex-web dev"
  },
  "engines": {
    "node": ">=20.11.1"
  }
}
