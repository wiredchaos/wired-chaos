// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

/**
 * Vanilla placeholder. For encrypted gating, see ConfidentialHookFHE.sol
 * which imports FHEVM TFHE library and uses encrypted types.
 * Keeping this file minimal preserves clean builds without FHE deps.
 */
contract ConfidentialHook {
    // Intentionally empty: reserved for encrypted trait checks / reveals.
}
